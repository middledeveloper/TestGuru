<tr>
  <td><%= question.text %></td>
  <td><%= link_to 'Show', admin_question_path(question.id) %></td>
  <td><%= link_to 'Edit', edit_admin_question_path(question.id) %></td>
  <td>
    <%= link_to 'Delete',
      admin_question_path(question),
      method: :delete,
      data: { confirm: 'Delete it?' }
    %>
  </td>
</tr>
