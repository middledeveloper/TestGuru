<tr>
  <td><%= question.text %></td>
  <td><%= link_to t('.info'), admin_question_path(question.id) %></td>
  <td><%= link_to t('.edit'), edit_admin_question_path(question.id) %></td>
  <td>
    <%= link_to t('.remove'),
      admin_question_path(question),
      method: :delete,
      data: { confirm: t('.confirm') }
    %>
  </td>
</tr>
