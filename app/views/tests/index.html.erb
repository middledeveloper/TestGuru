<% if flash[:info] %>
  <%= flash_message(:info) %>
<% end %>

<div class="mt-3">
  <h1><%= t('.header') %></h1>

  <table class="table">
    <thead class="thead-light">
      <tr>
        <th class="col"><%= t('.title') %></th>
        <th class="col"><%= t('.level') %></th>
        <th class="col"><%= t('.questions') %></th>
        <th class="col"></th>
      </tr>
    </thead>
    <%= render @tests %>
  </table>
</div>
