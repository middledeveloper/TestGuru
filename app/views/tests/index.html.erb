<% if flash[:info] %>
  <%= flash_message(:info) %>
<% end %>

<h1><%= t('.header') %></h1>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions') %></th>
    <th></th>
  </tr>
  <%= render @tests %>
</table>
