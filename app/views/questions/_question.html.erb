<tr>
  <td><%= question.text %></td>
  <td><%= link_to 'Edit', edit_question_path(question.id) %></td>
  <td><%= link_to 'Delete',
          question_path(question),
          method: 'delete',
          data: { confirm: 'Delete it?' } %></td>
</tr>
